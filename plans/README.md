# Implementation Plans

Generated by the improve skill on 2026-06-17. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

**Planned at repo commit:** `0a9cb55`

## Tracks

| Track | Goal | Plans |
|-------|------|-------|
| **Portfolio** | CI, reliable recurring, trends | 001 → 004 → 006 |
| **Personal use** | Import, budget warnings, recurring income | 002, 003, 005 (003 before 004) |
| **Stretch / thesis** | Full analytics, true multi-currency | 007, 008 |

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Add CI baseline and typecheck script | P1 | S | — | DONE |
| 002 | Extend budget alerts to warning threshold | P1 | S | 001 | DONE |
| 003 | Add recurring income (generalize recurring transactions) | P1 | M | 001 | DONE |
| 004 | Move recurring processing to Vercel Cron | P1 | M | 003 | DONE |
| 005 | Add CSV import mirroring export format | P1 | M | 001 | TODO |
| 006 | Add monthly trends chart | P2 | M | 001 | TODO |
| 007 | Add full analytics module with docs | P3 | L | 006 | TODO |
| 008 | True per-transaction multi-currency | P3 | L | 005 | TODO |

Status values: `TODO` | `IN PROGRESS` | `DONE` | `BLOCKED (reason)` | `REJECTED (reason)`

## Dependency graph

```
001 (CI)
 ├── 002 (budget warnings)
 ├── 003 (recurring income)
 │    └── 004 (cron)
 ├── 005 (CSV import)
 │    └── 008 (multi-currency)
 └── 006 (trends chart)
      └── 007 (full analytics)
```

- **001** must land first — every other plan assumes `npm run typecheck` and GitHub Actions exist.
- **003** before **004** — cron should process income and expense recurring rows.
- **005** before **008** — import semantics are simpler while all amounts are PLN-base.
- **006** before **007** — full analytics extends the trends service and chart patterns.

## Parallelization hints

After **001** completes, these can run in parallel on separate branches:

- 002 + 003 + 005 + 006 (no file overlap except shared types; prefer separate branches and merge 001 first)

## Findings considered and rejected

- **Shared household budgets**: requires new auth/data model beyond current per-user RLS; out of scope for this plan set.
- **AI transaction categorization**: no repo signal (stubs, flags); greenfield, not adjacent.
- **Email/push budget notifications**: deferred; plan 002 delivers in-app warnings only.
